# Implement one accepted staff UI task

Confirm task ID, accepted API contract, allowed paths, and verification. Preserve NgModule and repository conventions. Make surgical changes, run targeted tests and `npm run build`, and report evidence. Stop for an unapproved backend contract, permission, provider, or production action.

